#ifndef IMAGE_GRIS
#define IMAGE_GRIS

typedef struct {       // Nombre de colonnes
    double **data;    // Tableau 2D dynamique contenant les valeurs de la liste
} t_image_gris;

void negatif();
#endif